import { DisableDefaultRightMouseClickDirective } from './disable-default-right-mouse-click.directive';

describe('DisableDefaultRightMouseClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDefaultRightMouseClickDirective();
    expect(directive).toBeTruthy();
  });
});
