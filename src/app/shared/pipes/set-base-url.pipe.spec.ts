import { SetBaseUrlPipe } from './set-base-url.pipe';

describe('SetBaseUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SetBaseUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
